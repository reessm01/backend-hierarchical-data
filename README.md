# backend-hierarchical-data

*Example program that practices recursive hierarchal trees using django-mptt.*

To install (pipenv assumed to be installed)
Run:
`pipenv install`
`pipenv shell`

To launch
Run within Shell:
`python manage.py runserver`